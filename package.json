{
  "scripts": {
    "server-dev": "npx rimraf *.js && npx nodemon backend/app.ts",
    "client-dev": "npx tsc --watch --project public",
    "prestart": "npx tsc && npx tsc --project public",
    "start": "node app.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-status-codes": "^2.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^17.0.45",
    "nodemon": "^2.0.22",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
